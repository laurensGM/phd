---
import BaseLayout from '../layouts/BaseLayout.astro';
import researchObjectives from '../data/research-objectives.json';
import supervisors from '../data/supervisor.json';
import HomeDashboard from '../components/HomeDashboard';
import { abbreviateObjectiveText } from '../lib/abbreviateObjectiveText';
import '../styles/recent-activity.css';

type Objective = { type: string; status: string; objective: string };
type Supervisor = { name: string; initials?: string; title: string; profileUrl: string };

const primaryObjective = (researchObjectives as Objective[]).find(
  (o) => o.type === 'primary' && o.status === 'current'
);
const primaryObjectiveText = primaryObjective
  ? abbreviateObjectiveText(primaryObjective.objective)
  : '';

const supervisorList = supervisors as Supervisor[];
const base = import.meta.env.BASE_URL;
---

<BaseLayout title="Home" description="PhD Operating System - Your research topic and stats" deferLoader wide>
  <div class="homepage">
    <h1>PhD Manager</h1>
    <p class="tagline">Organizing theory, constructs, and literature for doctoral research</p>

    <section class="home-context-widget" aria-label="Research objective and supervisors">
      <div class="home-objective-card">
        <div class="home-context-card-head">
          <h2 class="home-context-title">Primary research objective</h2>
          <a href={`${base}research-questions/objectives/`} class="home-context-link">Objectives →</a>
        </div>
        {primaryObjectiveText ? (
          <p class="home-objective-text">{primaryObjectiveText}</p>
        ) : (
          <p class="home-objective-text home-objective-empty">No current primary objective set.</p>
        )}
      </div>

      <div class="home-supervisors-card">
        <div class="home-context-card-head">
          <h2 class="home-context-title">Supervisors</h2>
          <a href={`${base}supervisor/`} class="home-context-link">Profiles →</a>
        </div>
        <ul class="home-supervisors-list">
          {supervisorList.map((s) => {
            const initials =
              s.initials ||
              s.name
                .replace(/^Prof\s+/i, '')
                .split(/[\s-]+/)
                .filter(Boolean)
                .map((w) => w[0])
                .join('')
                .slice(0, 3)
                .toUpperCase();
            return (
              <li class="home-supervisor-item">
                <a href={`${base}supervisor/`} class="home-supervisor-link" title={s.title}>
                  <span class="home-supervisor-avatar" aria-hidden="true">{initials}</span>
                  <span class="home-supervisor-meta">
                    <span class="home-supervisor-name">{s.name}</span>
                    <span class="home-supervisor-initials-label">{initials}</span>
                  </span>
                </a>
              </li>
            );
          })}
        </ul>
      </div>
    </section>

    <section class="home-dashboard-section">
      <HomeDashboard client:load />
    </section>
  </div>
</BaseLayout>

<style>
  .homepage {
    max-width: none;
    width: 100%;
  }

  .homepage h1 {
    color: var(--color-maroon);
  }

  .tagline {
    font-size: 1.1rem;
    color: var(--color-ink-muted);
    margin-bottom: 2rem;
  }

  .home-context-widget {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
    gap: 1rem;
    margin-bottom: 1.75rem;
  }

  .home-objective-card,
  .home-supervisors-card {
    background: #fff;
    border: 1px solid var(--color-border);
    border-radius: 12px;
    padding: 1.15rem 1.25rem;
  }

  .home-context-card-head {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 0.75rem;
    margin-bottom: 0.85rem;
  }

  .home-context-title {
    margin: 0;
    font-family: var(--font-sans);
    font-size: 0.72rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--color-maroon);
    white-space: nowrap;
    flex-shrink: 0;
  }

  .home-context-link {
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--color-maroon);
    white-space: nowrap;
  }

  .home-context-link:hover {
    text-decoration: underline;
    color: var(--color-maroon-dark);
  }

  .home-objective-text {
    margin: 0;
    font-family: var(--font-serif);
    font-size: 1.15rem;
    line-height: 1.5;
    color: var(--color-ink);
  }

  .home-objective-empty {
    color: var(--color-ink-muted);
    font-style: italic;
  }

  .home-supervisors-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .home-supervisor-item {
    margin: 0;
  }

  .home-supervisor-link {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    text-decoration: none;
    color: inherit;
    padding: 0.35rem 0.25rem;
    border-radius: 8px;
    transition: background 0.15s;
  }

  .home-supervisor-link:hover {
    background: rgba(113, 32, 56, 0.05);
    text-decoration: none;
    color: inherit;
  }

  .home-supervisor-avatar {
    flex-shrink: 0;
    width: 2.5rem;
    height: 2.5rem;
    border-radius: 50%;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.02em;
    color: #fff;
    background: var(--color-maroon);
    border: 2px solid var(--color-gold);
  }

  .home-supervisor-meta {
    display: flex;
    flex-direction: column;
    gap: 0.1rem;
    min-width: 0;
  }

  .home-supervisor-name {
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--color-ink);
    line-height: 1.3;
  }

  .home-supervisor-initials-label {
    font-size: 0.8rem;
    color: var(--color-ink-muted);
    font-weight: 600;
  }

  @media (max-width: 800px) {
    .home-context-widget {
      grid-template-columns: 1fr;
    }
  }

  .home-dashboard-section {
    margin-top: 0.5rem;
  }

  :global(.home-dashboard) {
    margin-bottom: 2rem;
  }

  :global(.home-dashboard-loading),
  :global(.home-dashboard-error) {
    padding: 1rem;
    color: var(--color-ink-muted);
  }

  :global(.home-dashboard-error) {
    color: var(--color-error, #b91c1c);
    background: #fef2f2;
    border-radius: 8px;
  }

  /* Inline "total" badge used inside widget headers */
  :global(.home-widget-total) {
    display: inline-flex;
    align-items: baseline;
    gap: 0.4rem;
    padding: 0.35rem 0.7rem;
    background: var(--color-bg-alt);
    border: 1px solid var(--color-border);
    border-radius: 999px;
    text-decoration: none;
    color: inherit;
    white-space: nowrap;
    flex-shrink: 0;
    transition: border-color 0.2s, background 0.2s;
  }

  :global(a.home-widget-total:hover) {
    border-color: var(--color-maroon-muted);
    background: #fff;
    text-decoration: none;
    color: inherit;
  }

  :global(.home-widget-total-value) {
    font-family: var(--font-serif);
    font-size: 1.35rem;
    font-weight: 700;
    line-height: 1;
    color: var(--color-maroon);
  }

  :global(.home-widget-total-label) {
    font-size: 0.78rem;
    color: var(--color-ink-muted);
  }

  :global(.home-widget-total--sm .home-widget-total-value) {
    font-size: 1.1rem;
  }

  :global(.home-widget-total--sm) {
    padding: 0.25rem 0.6rem;
  }

  /* Research pipeline funnel */
  :global(.home-pipeline-section) {
    background: #fff;
    border: 1px solid var(--color-border);
    border-radius: 12px;
    padding: 1.5rem;
    margin-bottom: 1.5rem;
  }

  :global(.home-pipeline-header) {
    margin-bottom: 1.1rem;
  }

  :global(.home-pipeline-header .home-section-title) {
    margin-bottom: 0.35rem;
  }

  :global(.home-pipeline-note) {
    margin: 0;
    font-size: 0.85rem;
    color: var(--color-ink-muted);
    line-height: 1.45;
    max-width: 46rem;
  }

  :global(.home-pipeline-funnel) {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  :global(.home-pipeline-link) {
    display: grid;
    grid-template-columns: minmax(7rem, 11rem) minmax(0, 1fr) 2.75rem;
    align-items: center;
    gap: 0.75rem;
    padding: 0.3rem 0.35rem;
    border-radius: 8px;
    text-decoration: none;
    color: inherit;
    transition: background 0.15s;
  }

  :global(.home-pipeline-link:hover) {
    background: var(--color-bg-alt);
    text-decoration: none;
    color: inherit;
  }

  :global(.home-pipeline-label) {
    font-size: 0.85rem;
    font-weight: 500;
    color: var(--color-ink);
    line-height: 1.3;
  }

  :global(.home-pipeline-track) {
    display: flex;
    justify-content: center;
    min-width: 0;
  }

  :global(.home-pipeline-bar) {
    display: flex;
    align-items: center;
    justify-content: center;
    height: 2rem;
    min-width: 2.25rem;
    border-radius: 6px;
    transition: width 0.3s ease;
  }

  :global(.home-pipeline-count) {
    font-family: var(--font-serif);
    font-size: 1.05rem;
    font-weight: 700;
    color: #fff;
    padding: 0 0.4rem;
  }

  :global(.home-pipeline-share) {
    font-size: 0.78rem;
    font-weight: 600;
    color: var(--color-ink-muted);
    font-variant-numeric: tabular-nums;
    text-align: right;
  }

  @media (max-width: 560px) {
    :global(.home-pipeline-link) {
      grid-template-columns: minmax(5.5rem, 8rem) minmax(0, 1fr) 2.25rem;
      gap: 0.5rem;
    }
  }

  :global(.home-section-title) {
    font-family: var(--font-serif);
    font-size: 1.15rem;
    font-weight: 600;
    margin: 0 0 1rem 0;
    color: var(--color-maroon);
    line-height: 1.3;
  }

  :global(.home-top-row) {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
    gap: 1rem;
    margin-bottom: 1.75rem;
    align-items: stretch;
  }

  @media (max-width: 800px) {
    :global(.home-top-row) {
      grid-template-columns: 1fr;
    }
  }

  :global(.home-tasks-section) {
    margin: 0;
    padding: 1.15rem 1.25rem;
    background: #fff;
    border: 1px solid var(--color-border);
    border-radius: 12px;
    display: flex;
    flex-direction: column;
    min-width: 0;
  }

  :global(.home-tasks-header) {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 0.85rem;
  }

  :global(.home-tasks-header .home-section-title) {
    margin: 0;
  }

  :global(.home-tasks-chart) {
    display: flex;
    flex-direction: column;
    flex: 1;
    min-height: 0;
  }

  :global(.home-tasks-bars) {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    align-items: flex-end;
    gap: 0.5rem;
    flex: 1;
    min-height: 5rem;
  }

  :global(.home-tasks-bar-col) {
    flex: 1 1 0;
    min-width: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    height: 100%;
  }

  :global(.home-tasks-bar-stack) {
    flex: 1;
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-end;
    gap: 0.25rem;
    min-height: 0;
  }

  :global(.home-tasks-bar-label) {
    margin-top: 0.45rem;
    color: var(--color-ink);
    font-size: 0.72rem;
    font-weight: 500;
    text-align: center;
    line-height: 1.2;
  }

  :global(.home-tasks-bar-fill) {
    display: block;
    width: 72%;
    max-width: 2.75rem;
    border-radius: 6px 6px 0 0;
    min-height: 4px;
    transition: height 0.2s ease;
  }

  :global(.home-tasks-bar-fill-empty) {
    min-height: 3px;
    opacity: 0.2;
  }

  :global(.home-tasks-bar-count) {
    font-weight: 600;
    color: var(--color-ink-muted);
    font-variant-numeric: tabular-nums;
    font-size: 0.78rem;
    line-height: 1;
  }

  :global(.home-tasks-bar-total) {
    margin: 0.5rem 0 0;
    padding-top: 0.5rem;
    border-top: 1px solid var(--color-border);
    font-size: 0.8rem;
    color: var(--color-ink-muted);
  }

  :global(.home-timeline-section) {
    margin: 0;
    padding: 1.15rem 1.25rem;
    background: #fff;
    border: 1px solid var(--color-border);
    border-radius: 12px;
    min-width: 0;
  }

  :global(.home-timeline-header) {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 0.75rem;
    margin-bottom: 0.85rem;
  }

  :global(.home-timeline-header .home-section-title) {
    margin: 0;
  }

  /* Papers by publication year histogram */
  :global(.home-papers-histogram-section) {
    background: white;
    border: 1px solid var(--color-border);
    border-radius: 12px;
    padding: 1.5rem;
    margin-bottom: 1.5rem;
  }

  :global(.home-papers-histogram-header) {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 1rem;
  }

  :global(.home-papers-histogram-heading) {
    min-width: 0;
  }

  :global(.home-papers-histogram-header .home-section-title) {
    margin-bottom: 0.35rem;
  }

  :global(.home-papers-histogram-subtitle),
  :global(.home-papers-histogram-empty) {
    margin: 0;
    font-size: 0.85rem;
    color: var(--color-ink-muted);
    line-height: 1.45;
  }

  :global(.home-papers-histogram-wrap) {
    display: grid;
    grid-template-columns: auto 1fr;
    grid-template-rows: 1fr auto;
    gap: 0.35rem 0.5rem;
    align-items: stretch;
  }

  :global(.home-papers-histogram-y-label) {
    grid-row: 1;
    grid-column: 1;
    writing-mode: vertical-rl;
    transform: rotate(180deg);
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--color-ink-muted);
    text-align: center;
    justify-self: center;
    padding: 0.25rem 0;
  }

  :global(.home-papers-histogram-chart) {
    grid-row: 1;
    grid-column: 2;
    display: flex;
    gap: 0.5rem;
    min-height: 200px;
    overflow-x: auto;
    padding-bottom: 0.25rem;
  }

  :global(.home-papers-histogram-x-axis-label) {
    grid-row: 2;
    grid-column: 2;
    text-align: center;
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--color-ink-muted);
    padding-top: 0.15rem;
  }

  :global(.home-papers-histogram-y-axis) {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    flex-shrink: 0;
    width: 1.75rem;
    padding-bottom: 1.35rem;
    font-size: 0.72rem;
    color: var(--color-ink-muted);
    font-variant-numeric: tabular-nums;
    text-align: right;
  }

  :global(.home-papers-histogram-y-tick) {
    line-height: 1;
  }

  :global(.home-papers-histogram-plot) {
    position: relative;
    flex: 1;
    min-width: min(100%, 480px);
  }

  :global(.home-papers-histogram-grid) {
    position: absolute;
    inset: 0 0 1.35rem 0;
    pointer-events: none;
  }

  :global(.home-papers-histogram-gridline) {
    position: absolute;
    left: 0;
    right: 0;
    border-top: 1px dashed var(--color-border);
  }

  :global(.home-papers-histogram-bars) {
    position: relative;
    z-index: 1;
    display: flex;
    align-items: flex-end;
    gap: 0.35rem;
    height: 100%;
    min-height: 200px;
    padding-bottom: 1.35rem;
  }

  :global(.home-papers-histogram-bar-col) {
    flex: 1 1 0;
    min-width: 2rem;
    max-width: 3.5rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    height: 100%;
  }

  :global(.home-papers-histogram-bar-stack) {
    flex: 1;
    width: 100%;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    min-height: 0;
  }

  :global(.home-papers-histogram-bar) {
    width: 100%;
    min-height: 3px;
    background: var(--color-maroon);
    border-radius: 4px 4px 0 0;
    position: relative;
    display: flex;
    justify-content: center;
    transition: background 0.15s;
  }

  :global(.home-papers-histogram-bar:hover) {
    background: var(--color-maroon-dark);
  }

  :global(.home-papers-histogram-bar-empty) {
    min-height: 0;
    background: transparent;
    pointer-events: none;
  }

  :global(.home-papers-histogram-bar-empty:hover) {
    background: transparent;
  }

  :global(.home-papers-histogram-bar-value) {
    position: absolute;
    top: -1.1rem;
    font-size: 0.68rem;
    font-weight: 600;
    color: var(--color-ink-muted);
    font-variant-numeric: tabular-nums;
    white-space: nowrap;
  }

  :global(.home-papers-histogram-x-label) {
    margin-top: 0.35rem;
    font-size: 0.72rem;
    color: var(--color-ink-muted);
    font-variant-numeric: tabular-nums;
    white-space: nowrap;
  }

  /* Paper field / journal pie charts */
  :global(.home-paper-pies-section) {
    background: white;
    border: 1px solid var(--color-border);
    border-radius: 12px;
    padding: 1.5rem;
    margin-bottom: 1.5rem;
  }

  :global(.home-paper-pies-header) {
    margin-bottom: 1.25rem;
  }

  :global(.home-paper-pies-header .home-section-title) {
    margin-bottom: 0.35rem;
  }

  :global(.home-paper-pies-note) {
    margin: 0;
    font-size: 0.85rem;
    color: var(--color-ink-muted);
    line-height: 1.45;
  }

  :global(.home-paper-pies-grid) {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 1.5rem;
  }

  :global(.home-paper-pie-card) {
    border: 1px solid var(--color-border);
    border-radius: 10px;
    padding: 1.25rem;
    background: var(--color-bg-alt);
  }

  :global(.home-paper-pie-title) {
    margin: 0 0 0.25rem 0;
    font-family: var(--font-serif);
    font-size: 1rem;
    font-weight: 600;
    color: var(--color-maroon);
  }

  :global(.home-paper-pie-subtitle) {
    margin: 0 0 1rem 0;
    font-size: 0.78rem;
    color: var(--color-ink-muted);
    line-height: 1.4;
  }

  :global(.home-paper-pie-empty) {
    margin: 0;
    font-size: 0.875rem;
    color: var(--color-ink-muted);
  }

  :global(.home-paper-pie-body) {
    display: flex;
    flex-wrap: wrap;
    align-items: flex-start;
    gap: 1.25rem;
  }

  :global(.home-paper-pie-chart) {
    width: 150px;
    height: 150px;
    border-radius: 50%;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  :global(.home-paper-pie-hole) {
    width: 92px;
    height: 92px;
    border-radius: 50%;
    background: white;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    box-shadow: inset 0 0 0 1px var(--color-border);
  }

  :global(.home-paper-pie-value) {
    font-size: 1.4rem;
    font-weight: 700;
    line-height: 1.1;
    color: var(--color-ink);
  }

  :global(.home-paper-pie-label) {
    font-size: 0.65rem;
    color: var(--color-ink-muted);
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }

  :global(.home-paper-pie-legend) {
    list-style: none;
    margin: 0;
    padding: 0;
    flex: 1;
    min-width: 160px;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
    max-height: 240px;
    overflow-y: auto;
  }

  :global(.home-paper-pie-legend-item) {
    display: grid;
    grid-template-columns: 11px 1fr auto;
    align-items: center;
    gap: 0.45rem;
    font-size: 0.82rem;
  }

  :global(.home-paper-pie-legend-swatch) {
    width: 11px;
    height: 11px;
    border-radius: 3px;
  }

  :global(.home-paper-pie-legend-label) {
    color: var(--color-ink);
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  :global(.home-paper-pie-legend-count) {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    font-weight: 600;
    color: var(--color-ink);
    font-variant-numeric: tabular-nums;
    line-height: 1.2;
  }

  :global(.home-paper-pie-legend-pct) {
    font-size: 0.68rem;
    font-weight: 500;
    color: var(--color-ink-muted);
  }

  /* Snippet tag charts */
  :global(.home-snippet-charts-section) {
    background: white;
    border: 1px solid var(--color-border);
    border-radius: 12px;
    padding: 1.5rem;
    margin-bottom: 1.5rem;
  }

  :global(.home-snippet-charts-header) {
    margin-bottom: 1.25rem;
  }

  :global(.home-snippet-charts-header .home-section-title) {
    margin-bottom: 0.35rem;
  }

  :global(.home-snippet-charts-note) {
    margin: 0;
    font-size: 0.85rem;
    color: var(--color-ink-muted);
    line-height: 1.45;
  }

  :global(.home-snippet-charts-grid) {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
  }

  :global(.home-snippet-chart-card) {
    border: 1px solid var(--color-border);
    border-radius: 10px;
    padding: 1.25rem;
    background: var(--color-bg-alt);
  }

  :global(.home-snippet-chart-head) {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 0.75rem;
    margin-bottom: 1rem;
  }

  :global(.home-snippet-chart-title) {
    margin: 0 0 0.25rem 0;
    font-family: var(--font-serif);
    font-size: 1rem;
    font-weight: 600;
    color: var(--color-maroon);
  }

  :global(.home-snippet-chart-subtitle) {
    margin: 0;
    font-size: 0.8rem;
    color: var(--color-ink-muted);
  }

  :global(.home-snippet-chart-empty) {
    margin: 0;
    font-size: 0.875rem;
    color: var(--color-ink-muted);
  }

  :global(.home-snippet-chart-bars) {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    max-height: 280px;
    overflow-y: auto;
  }

  :global(.home-snippet-chart-bar-row) {
    display: grid;
    grid-template-columns: minmax(5rem, 38%) 1fr auto;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.82rem;
  }

  :global(.home-snippet-chart-bar-label) {
    color: var(--color-ink);
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  :global(.home-snippet-chart-bar-track) {
    height: 0.65rem;
    background: var(--color-bg-alt);
    border-radius: 999px;
    overflow: hidden;
  }

  :global(.home-snippet-chart-bar-fill) {
    display: block;
    height: 100%;
    border-radius: 999px;
    min-width: 3px;
    transition: width 0.2s ease;
  }

  :global(.home-snippet-chart-bar-count) {
    font-weight: 600;
    color: var(--color-ink-muted);
    font-variant-numeric: tabular-nums;
    min-width: 1.5rem;
    text-align: right;
  }

  /* Milestones timeline (shared item styles; section chrome is in .home-top-row) */
  :global(.home-top-row .home-timeline-section) {
    margin-top: 0;
  }

  :global(.home-timeline-link) {
    font-size: 0.875rem;
    color: var(--color-maroon);
    text-decoration: none;
  }

  :global(.home-timeline-link:hover) {
    text-decoration: underline;
    color: var(--color-maroon-dark);
  }

  :global(.home-timeline-empty) {
    margin: 0;
    font-size: 0.875rem;
    color: var(--color-ink-muted);
    line-height: 1.5;
  }

  :global(.home-timeline-countdown) {
    display: flex;
    align-items: baseline;
    gap: 0.5rem;
    margin-bottom: 1rem;
    padding: 0.6rem 0.85rem;
    background: linear-gradient(to right, rgba(113, 32, 56, 0.08), rgba(113, 32, 56, 0.02));
    border: 1px solid var(--color-border);
    border-left: 4px solid var(--color-maroon);
    border-radius: 8px;
    text-decoration: none;
    color: inherit;
    transition: border-color 0.2s;
  }

  :global(.home-timeline-countdown:hover) {
    border-color: var(--color-maroon-muted);
    border-left-color: var(--color-maroon);
    text-decoration: none;
    color: inherit;
  }

  :global(.home-timeline-countdown-value) {
    font-family: var(--font-serif);
    font-size: 1.75rem;
    font-weight: 700;
    line-height: 1;
    color: var(--color-maroon);
  }

  :global(.home-timeline-countdown-label) {
    font-size: 0.85rem;
    color: var(--color-ink);
    line-height: 1.3;
  }

  :global(.home-timeline) {
    list-style: none;
    margin: 0;
    padding: 0;
    position: relative;
  }

  :global(.home-timeline-item) {
    display: grid;
    grid-template-columns: auto 12px 1fr;
    gap: 0 0.75rem;
    padding-bottom: 1rem;
    position: relative;
    align-items: center;
  }

  :global(.home-timeline-item:last-child) {
    padding-bottom: 0;
  }

  :global(.home-timeline-item)::before {
    content: '';
    position: absolute;
    left: 5px;
    top: 22px;
    bottom: 0;
    width: 2px;
    background: var(--color-border);
  }

  :global(.home-timeline-item:last-child::before) {
    display: none;
  }

  :global(.home-timeline-date) {
    grid-column: 1;
    font-size: 0.8rem;
    color: var(--color-ink-muted);
    white-space: nowrap;
  }

  :global(.home-timeline-dot) {
    grid-column: 2;
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: var(--color-border);
    position: relative;
    z-index: 1;
  }

  :global(.home-timeline-content) {
    grid-column: 3;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  :global(.home-timeline-title) {
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--color-ink);
  }

  :global(.home-timeline-past) {
    opacity: 0.5;
  }

  :global(.home-timeline-past .home-timeline-date) {
    color: var(--color-border);
  }

  :global(.home-timeline-past .home-timeline-title) {
    color: var(--color-ink-muted);
    font-weight: 400;
  }

  :global(.home-timeline-past .home-timeline-dot) {
    background: var(--color-border);
    opacity: 1;
  }

  :global(.home-timeline-past::before) {
    background: var(--color-bg-alt);
  }

  :global(.home-timeline-future .home-timeline-title) {
    font-weight: 600;
    color: var(--color-ink);
  }

  :global(.home-timeline-future .home-timeline-date) {
    color: var(--color-ink);
    font-weight: 600;
  }

  :global(.home-timeline-future .home-timeline-dot) {
    background: var(--color-maroon-muted, #9f4d63);
    box-shadow: 0 0 0 2px rgba(113, 32, 56, 0.12);
  }

  :global(.home-timeline-next .home-timeline-dot) {
    background: var(--color-maroon);
    box-shadow: 0 0 0 3px rgba(128, 0, 0, 0.2);
  }

  :global(.home-timeline-next .home-timeline-title) {
    font-weight: 600;
    color: var(--color-maroon);
  }

  :global(.home-timeline-badge) {
    font-size: 0.7rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.03em;
    color: white;
    background: var(--color-maroon);
    padding: 0.15rem 0.5rem;
    border-radius: 4px;
  }
</style>
